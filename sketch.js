function clicked() {
    var thisElement = document.getElementById("list");
    console.log(thisElement)
    thisElement.hidden = true;

    alert("I told you you shouldn't ! Why don't you listen ?! Now the list is gone FOREVER !");
}